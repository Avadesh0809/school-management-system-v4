spring.application.name=school-management-system
server.port=8080

# ----- DataSource (MySQL) -----
spring.datasource.url=jdbc:mysql://localhost:3306/v4_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:787932}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# ----- JWT -----
app.jwt.secret=${JWT_SECRET:ZmQ4ZjFhYjY1NGM5NDU0ZjkyYjk5MGFmYjA4OGI4M2YyMjk0YzVmZGYxYTY0ZjVmZjE3ZjUyOWE3MWI4YzM0OQ==}
app.jwt.access-token-minutes=30
app.jwt.refresh-token-days=14

# ----- File storage -----
app.storage.root=${STORAGE_ROOT:./storage}
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=50MB

# ----- Mail -----
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USER:umaraiyaavadesh132@gmail.com}
spring.mail.password=${MAIL_PASS:uejl poce mmdl rqdc}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=${MAIL_FROM:umaraiyaavadesh132@gmail.com}
app.mail.enabled=${MAIL_ENABLED:true}

# ----- Cache -----
spring.cache.type=caffeine
spring.cache.cache-names=users,roles,students,teachers,classes,dashboard
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=5m

# ----- Logging -----
logging.level.root=INFO
logging.level.com.school.sms=DEBUG
logging.file.name=logs/school-management.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# ----- Swagger -----
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

management.endpoints.web.exposure.include=health,info,metrics
